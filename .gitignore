# ZCode 会话计划/临时文件
.zcode/

# 运行日志（由 install.sh/daemon 生成于源码目录的残留）
session-stats/daemon.log
session-stats/daemon.log.old
plugins/session-stats/daemon.log
plugins/session-stats/daemon.log.old

# 运行时产物（install.sh 部署到 ~/.zcode/session-stats/，不在仓库内）
# 若从仓库拷贝出源码目录，这些由安装脚本在运行时重新生成

# macOS
.DS_Store
