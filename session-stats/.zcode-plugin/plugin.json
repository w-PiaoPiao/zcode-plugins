{
  "name": "session-stats",
  "version": "0.2.0",
  "description": "Session token & performance stats: turns/steps, LLM time, TTFT, tok/s, cache hit, input/output tokens — shown below the chat composer via a lightweight status bar, plus a /stats command. The status bar itself requires install.sh (asar patch); this manifest registers hooks only.",
  "author": {
    "name": "yizhiwang"
  },
  "license": "MIT",
  "hooks": "hooks"
}
